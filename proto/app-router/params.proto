syntax = "proto3";
package Stridelabs.stride.app_router;

import "gogoproto/gogo.proto";

option go_package = "github.com/Stride-Labs/stride/v3/x/app-router/types";

// Params defines the parameters for the module.
// next id: 1
message Params {
  option (gogoproto.goproto_stringer) = false;
  // optionally, turn off this module
  // TODO: implement
  bool active = 1;
}