name: staticcheck

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  golangci:
    name: staticcheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/setup-go@v3
      - uses: actions/checkout@v3
        with:
          go-version: 1.18
        run: make staticcheck
        shell: bash
